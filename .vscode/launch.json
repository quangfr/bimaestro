{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Maestro (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Live Server"
    },
    {
      "name": "Launch Maestro (Edge)",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Start Live Server"
    }
  ]
}
